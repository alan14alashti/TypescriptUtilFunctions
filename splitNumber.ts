// export default function addCharThroughNumber(number: number, perChar: number = 1, replaceCharacter: string): string {
//   return number.toString().replace(new RegExp(`/\B(?=(\d{${perChar}})+(?!\d))/g`), replaceCharacter);
// }